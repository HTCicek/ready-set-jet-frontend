const defaultState = {
  flight_destination: '',
  flight_origin: '',
};

function reducer(prevState = defaultState, action) {
  switch (action.type) {
    default:
      return prevState;
  }
}

export default reducer;
